# Synthetic Sample Data

`example_client_kpis.csv` contains invented values for documentation, prototyping, and screenshot preparation. It is not an extract, transformation, or statistical derivative of any customer dataset.

The sample deliberately uses `example_client` and a compact KPI grain. It does not reproduce the production model or imply actual business performance.

